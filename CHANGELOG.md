# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.1.0] — 2026-09-11

首个完整版本。M0–M8 全部里程碑完成。

### 能力

- **图片 → Excel**：识别图片中的表格并生成带样式的 xlsx。离线引擎用文字框坐标
  几何重建表格，云端引擎可识别合并单元格。
- **图片 → 文字**：双引擎 OCR。默认 **RapidOCR 本地离线**（文件不出本机、零费用），
  可在设置里开启 **DeepSeek Vision 云端**（复杂表格、手写、版面理解更强）。
  云端请求带自适应切片与结果缓存，避免官方"单图 1300×1300 等效"缩放导致密集小字丢失。
- **PDF ↔ Word**：双向转换，PDF → Word 走 pdf2docx（保留文字、图片、表格）。
- **Office → PDF**：Word / Excel / PowerPoint 三级引擎路由 ——
  **Office COM（保真 100）→ LibreOffice（82）→ 内置渲染（55，永远可用）**。
- **PDF 工具箱**：合并、拆分、旋转、压缩、提取页面、转图片、文档信息。
- **PDF 水印**：矢量文字，任意缩放清晰、文件极小、文字仍可复制检索；
  支持平铺、任意角度、页码范围、每页不同内容、拖拽定位、实时预览。
- **图片水印**：文字 / Logo、平铺防盗图、每张唯一内容、EXIF 方向自动摆正。
- **扫描件转可搜索 PDF**：给扫描件叠加不可见文字层，外观不变但可搜索、可复制。
- **多步骤流水线**：把任意动作串成一条链，上一步的产物直接喂给下一步。
  开工前先校验整条链的类型匹配；中间产物写在临时目录并自动清理。
- **参数预设**：把调好的一套参数按动作存下来复用，同名即覆盖。
- **命令行**：`actions` / `run` / `pipeline` / `watch` / `presets` / `selfcheck` / `serve`。
  与界面共用同一个任务队列，并发、进度、失败隔离、输出重名策略完全一致。
- **拖拽导入**、**任务历史**、**首次启动环境自检**、**运行时密钥加密存储**（Windows DPAPI）。

### 工程约束

- 内核只绑 `127.0.0.1` + 随机端口 + 每次启动随机 Token，**Token 不进渲染进程**，
  所有内核调用经主进程 IPC 转发。
- 预览与正式输出**走同一套动作代码**，杜绝"预览好看、结果不对"。
- 失败隔离：单个坏文件只失败它自己，并在结果表里给出可读原因。
- 原子写入：先写临时文件再改名，输出目录里出现的文件一定是完整的。
- COM 自动化的三条硬约束：专用 STA 线程、看门狗超时强杀、
  **只清理"创建 COM 对象之后新出现的"进程**（绝不触碰用户自己开着的 Word/Excel）。
- 输出路径在并发下做**原子预定**，批量处理同名文件不会静默互相覆盖。

### 已知限制

- **云端 OCR 尚未用真实 API Key 联调**。请求构造与错误处理按官方文档实现并有测试覆盖，
  但没有跑过真实调用 —— 这是当前最主要的验证缺口。
- PDF 加密 / 解密 / 权限设置未实现。
- 未安装 Office 且未安装 LibreOffice 时，Excel / PPT 转 PDF 不可用
  （内置渲染不做 xlsx / pptx —— 只靠文字提取会丢失全部结构）。
- 「监听文件夹」只在命令行提供。它天然是常驻进程，而内核随应用启停；
  做成界面开关会变成"关掉应用就失效"的假功能。
- 仅在 Windows 上验证过。Electron 与 Python 内核本身是跨平台的，
  但文件关联、NSIS 安装包、Office COM 路由与 DPAPI 密钥存储都是 Windows 专用形态。

### 验证

- 内核单元测试 295 项
- 端到端冒烟测试 16 组
- 打包内核验证 11 组（含双层 PDF / 预设 / 流水线 / CLI）
- NSIS 安装包与便携版均已启动验证，关闭后无残留进程

### 依赖清单

`core/requirements.txt` 默认装齐全部能力（含本地离线 OCR，约 300 MB），
保证"照着 README 装完就能用"。`core/requirements-dev.txt` 另外包含测试与打包依赖。

> `core/tests/test_requirements.py` 会**静态扫描代码里 import 的每个第三方包并与
> 依赖清单比对**，漏写清单会直接测试失败。加这条测试是因为真实踩过：
> `pillow-heif` 长期没写进任何清单，导致新环境读不了 HEIC（iPhone 照片），
> 而 `image.watermark` / `image.ocr` 都声明接受 `.heic`。
