# 仓库内统一用 LF 存储文本，避免 Windows/Linux 协作时出现"整文件都改了"的 diff。
# 注意：Windows 脚本保留 CRLF —— cmd.exe 对 LF 结尾的批处理容错较差。
* text=auto eol=lf

*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# 二进制资源：不做任何换行转换
*.ico binary
*.png binary
*.jpg binary
*.exe binary
*.dll binary
*.pyd binary
*.ttf binary
*.ttc binary
*.onnx binary
